#include <stdio.h>

/**
 * Main - Write a function that prints the alphabet in lowercase
 *
 * Return: 0 (Success)
 */

int main(void)
{
int print_alphabet(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
}
