#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 *@a: integer input
 *@b: integer input
 * Return: an integer
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
