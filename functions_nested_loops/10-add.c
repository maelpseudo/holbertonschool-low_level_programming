#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and return the result
 *
 * result: return sum
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
