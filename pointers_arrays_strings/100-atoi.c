#include "main.h"
#include <stdio.h>

/**
 * 
 * 
 * 
*/

int _atoi(char *s)
{
    int i;;
    int var;
    int length = strlen(s);

    for (i = 0; i < length; i++)
    {
        var =+ s[i];
    }
}