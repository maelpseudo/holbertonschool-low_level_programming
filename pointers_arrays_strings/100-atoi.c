#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * 
 * 
 * 
*/

int _atoi(char *s)
{
    int i;
    int var;
    int length = strlen(s);

    for (i = 0; i < length; i++)
    {
        var = var + s[i];
    }
    return (0);
}