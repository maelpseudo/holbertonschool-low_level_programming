#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function that return the lenght of a string
 * @s: input integer
 * Return: lenght of s
*/

int _strlen(char *s)
{
        return(strlen(s));
}
